[project]
name = "ontologica-os-public"
version = "1.0.0rc1"
description = "Noncanonical public proofing demo for Ontologica OS vocabulary."
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
